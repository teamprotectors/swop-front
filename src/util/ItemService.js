import ItemObject from "./Item";

/*class ItemObjectService {
    itemsObjects: ItemObject[] = [];

    /**
     * Agrega un Item en la lista, si ya existe uno con la misma cédula genera un error.
     * @param item ItemObject a actualizar.
     *//*
    agregar(item: ItemObject): void {
        if (!this.existeItem(item.getId())) {
            this.itemsObjects[item.getId()] = item;
        } else {
            console.error('Error agregando el item: El item ya existe');
        }
    }

    /**
     * Valida si existe o no un item.
     * @param id Id del item a buscar.
     *//*
    existeItem(id: string): boolean {
        return this.itemsObjects[id];
    }
}

export default ItemObjectService;
*/
